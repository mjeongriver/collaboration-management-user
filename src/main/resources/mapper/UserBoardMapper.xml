<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.choongang.scheduleproject.board.service.UserBoardMapper">

	<!-- 프로젝트 리스트 출력 + 검색 -->
	<select id="getList" resultType="UserBoardVO">
		select board_num,
		board_category,
		board_title,
		board_writer,
		board_regdate,
		board_startdate,
		board_enddate,
		board_process
		from user_board
		where pj_num = #{pjNum}
		<choose>
			<when test='cri.searchType == "boardCategory" and cri.search != null'> 
				where board_category like concat('%', #{cri.search}, '%')
			</when>
			<when test='cri.searchType == "boardTitle" and cri.search != null'> 
				where board_title like concat('%', #{cri.search}, '%')
			</when>
			<when test='cri.searchType == "boardWriter" and cri.search != null'> 
				where board_writer like concat('%', #{cri.search}, '%')
			</when>
			<when test='cri.searchType == "boardRegdate" and cri.search != null'> 
				where board_regdate like concat('%', #{cri.search}, '%')
			</when>
			<when test='cri.searchType == "boardStartdate" and cri.search != null'> 
				where board_startdate like concat('%', #{cri.search}, '%')
			</when>
			<when test='cri.searchType == "boardEnddate" and cri.search != null'> 
				where board_enddate like concat('%', #{cri.search}, '%')
			</when>
		</choose>
		order by pj_num desc
		limit #{cri.pageStart}, #{cri.amount}
	</select>
	
	<!-- 검색 결과의 갯수 -->
	<select id="getCount" resultType="int">
		select count(*) as total from user_board
		where pj_num = #{pjNum}
		<choose>
			<when test='cri.searchType == "boardCategory" and cri.search != null'> 
				where board_category like concat('%', #{cri.search}, '%')
			</when>
			<when test='cri.searchType == "boardTitle" and cri.search != null'> 
				where board_title like concat('%', #{cri.search}, '%')
			</when>
			<when test='cri.searchType == "boardWriter" and cri.search != null'> 
				where board_writer like concat('%', #{cri.search}, '%')
			</when>
			<when test='cri.searchType == "boardRegdate" and cri.search != null'> 
				where board_regdate like concat('%', #{cri.search}, '%')
			</when>
			<when test='cri.searchType == "boardStartdate" and cri.search != null'> 
				where board_startdate like concat('%', #{cri.search}, '%')
			</when>
			<when test='cri.searchType == "boardEnddate" and cri.search != null'> 
				where board_enddate like concat('%', #{cri.search}, '%')
			</when>
		</choose>
	</select>

</mapper>